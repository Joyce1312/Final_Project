# .env.example
# Copy this file to `.env` and fill in real values for local development.
# On Render or Heroku, set these in the dashboard under Environment Variables.

# Flask secret key (used for sessions, CSRF protection, etc.)
SECRET_KEY=change-me

# Flask session type (filesystem works locally; on production you might change)
SESSION_TYPE=filesystem


DATABASE_URL=sqlite:///instance/ecommerce.db
